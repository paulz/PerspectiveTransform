source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Example' do
  pod 'PerspectiveTransform', :path => '../'
end

target 'Tests' do
  pod 'PerspectiveTransform', :path => '../'

  pod 'Quick'
  pod 'Nimble'
  pod 'Nimble-Snapshots'
end
